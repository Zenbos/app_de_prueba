import 'package:flutter/material.dart';

class iCCID extends StatelessWidget{
  const iCCID ({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context){
    return const Center(
      child: Text('ICCID'),
    );
  }
}