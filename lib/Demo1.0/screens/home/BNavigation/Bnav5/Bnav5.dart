import 'package:flutter/material.dart';

class Bnav5 extends StatelessWidget{
  const Bnav5 ({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context){
    return const Center(
      child: Text('Shop'),
    );
  }
}