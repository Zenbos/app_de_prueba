import 'package:flutter/material.dart';

class Bnav2 extends StatelessWidget{
  const Bnav2 ({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context){
    return const Center(
      child: Text('Coming Soon'),
    );
  }
}